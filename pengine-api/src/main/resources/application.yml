spring:
  messages:
    basename: i18n/messages

promotion:
  file-dir: D:\Workspace\pengine\data\PromotionCase
  file-prefix: promotion
  file-suffix: .json

response:
  prettify: false

logging:
  level:
    org.springframework: debug
